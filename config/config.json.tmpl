{
  "ch_url":           "${CH_URL}",
  "mcp_url":          "${MCP_URL}",
  "spa_origin":       "${SPA_ORIGIN}",
  "db":               "${DB}",
  "brand_name":       "${BRAND_NAME}",
  "accent":           "${ACCENT}",
  "oauth_client_id":  "${OAUTH_CLIENT_ID}",
  "oauth_audience":   "${OAUTH_AUDIENCE}",
  "oauth_organization": "${OAUTH_ORGANIZATION}"
}
